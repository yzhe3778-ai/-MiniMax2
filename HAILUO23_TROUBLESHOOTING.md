# 🔧 Hailuo 2.3 问题排查指南

## ❌ 错误：API 响应中未包含 task_id

### 📋 官方文档

根据 [MiniMax 官方文档](https://platform.minimax.io/docs/api-reference/video-generation-i2v)，正确的响应格式应该是：

```json
{
  "task_id": "106916112212032",
  "base_resp": {
    "status_code": 0,
    "status_msg": "success"
  }
}
```

### 🔍 常见原因和解决方案

#### 1. ✅ 分辨率参数错误（已修复）

**问题**: 使用了 `720P`，但文档要求 `768P`

**官方支持的分辨率**:
| 模型 | 6秒 | 10秒 |
|------|-----|------|
| MiniMax-Hailuo-2.3 | **768P** (默认), 1080P | **768P** (默认) |
| MiniMax-Hailuo-2.3-Fast | **768P** (默认), 1080P | **768P** (默认) |

**修复**: 已将所有 `720P` 改为 `768P`

#### 2. 🔑 API Key 未配置

**检查**: 在 Vercel 环境变量中是否配置了 `MINIMAX_API_KEY`

**解决**:
1. 访问 Vercel 项目设置
2. 进入 "Environment Variables"
3. 添加 `MINIMAX_API_KEY`
4. 重新部署

#### 3. 📸 图片格式问题

**官方要求**:
- 格式：JPG, JPEG, PNG, WebP
- 大小：< 20MB
- 尺寸：短边 > 300px，长宽比 2:5 到 5:2

**已优化**:
- 自动检测 MIME 类型
- 支持 Base64 Data URL 格式
- 添加图片大小日志

#### 4. 🔗 API 端点错误

**正确的端点**:
```
https://api.minimax.io/v1/video_generation
```

**检查代码**: 已确认使用正确的端点

#### 5. 📝 请求参数格式

**必需参数**（文字转视频）:
```json
{
  "prompt": "视频描述",
  "model": "MiniMax-Hailuo-2.3",
  "duration": 6,
  "resolution": "768P"
}
```

**必需参数**（图片转视频）:
```json
{
  "first_frame_image": "data:image/jpeg;base64,...",
  "model": "MiniMax-Hailuo-2.3",
  "duration": 6,
  "resolution": "768P"
}
```

### 🧪 测试步骤

#### 1. 测试文字转视频

```bash
curl --request POST \
  --url https://api.minimax.io/v1/video_generation \
  --header 'Authorization: Bearer 你的API_KEY' \
  --header 'Content-Type: application/json' \
  --data '{
    "prompt": "A mouse runs toward the camera, smiling and blinking.",
    "model": "MiniMax-Hailuo-2.3",
    "duration": 6,
    "resolution": "768P"
  }'
```

**预期响应**:
```json
{
  "task_id": "106916112212032",
  "base_resp": {
    "status_code": 0,
    "status_msg": "success"
  }
}
```

#### 2. 查看服务器日志

在 Vercel 部署日志中查看：

```
调用 MiniMax 视频生成 API...
Model: MiniMax-Hailuo-2.3
Duration: 6
Resolution: 768P
Prompt: 你的提示词
发送请求数据: {...}
HTTP 状态码: 200
原始响应文本: {...}
```

### 📊 错误代码对照表

根据官方文档，`base_resp.status_code` 的含义：

| 状态码 | 含义 | 解决方案 |
|--------|------|----------|
| 0 | 成功 | 正常 |
| **1008** | **余额不足** | **充值账户** ⚠️ |
| 2013 | 参数错误 | 检查参数格式 |
| 其他 | 其他错误 | 查看 status_msg |

### 💰 错误 1008：余额不足（最常见）

**错误响应**:
```json
{
  "task_id": "",
  "base_resp": {
    "status_code": 1008,
    "status_msg": "insufficient balance"
  }
}
```

**解决步骤**:

1. **登录 MiniMax 平台**
   - 访问：https://platform.minimaxi.com/
   - 使用你的账号登录

2. **查看账户余额**
   - 进入：用户中心 → 账户余额
   - 查看当前余额

3. **充值账户**
   - 点击"充值"按钮
   - 选择充值金额（建议至少充值 ¥50）
   - 完成支付

4. **重新测试**
   - 充值成功后立即生效
   - 返回网站重新生成视频

**定价参考**:
- 视频生成按时长和分辨率计费
- 具体价格请在 MiniMax 平台查看
- 建议充值后查看消费记录了解单次成本

### 🔧 调试检查清单

- [ ] API Key 已在 Vercel 配置
- [ ] API Key 有效且未过期
- [ ] 账户有足够余额
- [ ] 使用 768P 分辨率（不是 720P）
- [ ] 模型名称正确：`MiniMax-Hailuo-2.3`
- [ ] 时长为 6 或 10 秒
- [ ] 如果是图片转视频，图片格式符合要求
- [ ] 网络可以访问 api.minimax.io

### 💡 常见问题

#### Q1: 为什么响应是 500 错误？

**可能原因**:
1. API Key 未配置或无效
2. 账户余额不足
3. 参数格式错误
4. 网络问题

**解决**: 查看 Vercel 日志中的详细错误信息

#### Q2: 图片转视频一直失败？

**检查**:
1. 图片大小 < 20MB
2. 图片格式为 JPG/PNG/WebP
3. 图片尺寸符合要求（短边 > 300px）
4. 图片长宽比在 2:5 到 5:2 之间

#### Q3: 10秒视频生成失败？

**原因**: MiniMax-Hailuo-2.3 的 10秒模式只支持 768P

**解决**: 
- 使用 6秒 + 1080P
- 或使用 10秒 + 768P

#### Q4: 相机运镜不生效？

**检查**: 
1. 使用正确的命令格式：`[Push in]`
2. 命令在方括号内
3. 支持的15种命令之一

### 📝 相机运镜命令

根据官方文档，支持的15种命令：

```
Truck:    [Truck left], [Truck right]
Pan:      [Pan left], [Pan right]
Push:     [Push in], [Pull out]
Pedestal: [Pedestal up], [Pedestal down]
Tilt:     [Tilt up], [Tilt down]
Zoom:     [Zoom in], [Zoom out]
Shake:    [Shake]
Follow:   [Tracking shot]
Static:   [Static shot]
```

**组合使用**:
```
[Pan left,Pedestal up] 描述内容
```

**顺序使用**:
```
描述...[Push in], then...[Push out]
```

### 🔄 重新部署

修复后需要重新部署：

```bash
git add .
git commit -m "fix: 修复 Hailuo 2.3 分辨率参数"
git push origin master
```

Vercel 会自动部署（约 1-2 分钟）

### 📞 获取帮助

如果问题仍然存在：

1. **查看完整日志**
   - Vercel 控制台 → Deployments → 最新部署 → Functions
   - 查找 `/api/hailuo23/generate` 的日志

2. **检查 API 响应**
   - 查看 "原始响应文本" 日志
   - 确认 MiniMax 返回的完整内容

3. **联系 MiniMax 支持**
   - 访问：https://platform.minimax.io
   - 查看文档或联系技术支持

### ✅ 修复总结

已完成的修复：

1. ✅ 分辨率从 720P 改为 768P
2. ✅ 优化图片格式检测
3. ✅ 添加详细的调试日志
4. ✅ 改进错误处理
5. ✅ 更新所有相关文档

### 🎯 测试建议

部署后测试：

1. **文字转视频**（最简单）
   - 提示词：`A cat sitting on a chair`
   - 模型：MiniMax-Hailuo-2.3
   - 时长：6秒
   - 分辨率：768P

2. **图片转视频**
   - 上传清晰的图片
   - 添加简单描述
   - 使用相同配置

3. **查看日志**
   - 确认请求参数正确
   - 确认收到 task_id
   - 确认视频生成成功

---

**如果按照以上步骤操作后仍有问题，请提供完整的错误日志！**

